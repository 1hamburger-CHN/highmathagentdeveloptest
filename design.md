# Design: 复变函数苏格拉底教练 — 多智能体辅导系统

Generated by /office-hours on 2026-05-21
Branch: main
Repo: mathagentdevelop (第十五届中国软件杯)
Status: DRAFT
Mode: Builder

## Problem Statement

复变函数学生面临的核心问题不是"找不到学习资料"，而是**不知道自己哪里没真懂**。一个学生看完"极限定义"后能做课后题，但遇到需要真正理解 ε-δ 的证明题就卡住——因为他的理解停留在"越来越接近"的直觉层面，而非精确定义层面。传统课堂 1对50 模式下，老师即使发现学生没真懂也没有时间一对一追问到底。

目标：构建一个**苏格拉底式多智能体教练系统**，通过自然语言对话诊断学生的知识盲区，用追问逼迫学生直面自己"装懂"的概念，然后协同多个专业Agent生成针对该盲区的个性化学习资源，形成"诊断→追问→生成→评估→追踪"的完整教学闭环。

## What Makes This Cool

不是又一个"AI帮你生成学习资料"的工具。而是一个**能发现你哪不懂、追着问你直到你真的懂了、然后专门为你的盲区生成补救材料**的数学教练。

对评委的demo故事线：一个学生说"我觉得我懂极限了"→ 系统5分钟诊断发现他在"无穷小比较"上只是背了等价无穷小公式 → 苏格拉底追问"sin x 约等于 x 什么时候对，什么时候不对？" → 学生卡住 → 系统发现根因是"泰勒展开"这个概念没掌握 → 自动生成泰勒展开的针对性讲义+动画+练习 → 学生学完回来 → 再次追问，这次通过了 → 画像更新，学习路径自动调整。

评委看到的不是功能checklist，而是一个**认知转变的story arc**。

## Constraints

1. **比赛要求**：第十五届中国软件杯，科大讯飞出题。必须使用多智能体架构、科大讯飞Spark API、生成≥5种类型学习资源
2. **时间**：比赛截止前需交付可运行系统+7分钟演示视频+PPT+配套文档
3. **技术栈**：Python后端+TypeScript前端，Spark API作为主力LLM之一，DeepSeek辅助
4. **范围**：聚焦"极限与连续"一章（约5-8个核心概念点），做深做透
5. **质量要求**：数学准确性不可妥协（SymPy验证+知识库交叉校验），防幻觉机制

## Premises

1. 核心差异化在"诊断→追问→发现根因→生成针对性资源→追踪理解"闭环，而非单一苏格拉底对话
2. 聚焦"极限与连续"一章，做深而非做广——评委记住转变故事胜过功能数量
3. 多Agent架构服务于教学逻辑——不同Agent各有分工，协作产出的教学闭环单模型做不到
4. Demo说服力来自展示真实认知转变过程——一个story arc，而非功能列表
5. Spark API作为主力LLM之一，框架支持多模型路由，不锁定单一供应商

## Approaches Considered

### Approach A: 全链路闭环版（已选择）

完整LangGraph多Agent系统。9个专业Agent协作，FastAPI+Next.js全栈，ChromaDB知识库，Spark+DeepSeek双模型。

- 9个Agent：Orchestrator, Profile Builder, Diagnostician, Socratic Coach, Lecture Note, Exercise, Mind Map, Reading, Assessment（Quality Gate作为管线过滤器，不计入Agent数）
- 5+种资源：教学讲义、思维导图、分类型练习题、拓展阅读、代码可视化/动画脚本
- 完整"极限与连续"知识库（LaTeX-aware chunking + BGE-M3 embedding + 混合检索）
- 3维学习画像（v1精简版），动态更新

### Approach B: 对话诊断轻量版

Gradio单页应用，3Agent（诊断+追问+生成），2-3种资源类型。开发快但比赛"多智能体架构"得分点不足。

### Approach C: 深度追问极致版

语音+情绪检测+心流调控，极致追问体验。技术风险高（中文数学ASR可用性未知），且资源类型不足。

## Recommended Approach

**Approach A — 全链路闭环版**，理由：
- 覆盖比赛全部5项核心功能需求和45%的"功能实现"评分
- 多Agent架构本身就是技术亮点，评审认可度高
- "苏格拉底教练"叙事线贯穿整个架构，给系统注入灵魂

叠加Approach C的追问体验为加分项（流式输出思考过程、追问决策可视化、心流提示），在A的架构基础上做交互体验的精致打磨。

## Agent精简方案 (工程设计优化)

审查建议将9Agent合并为5Agent，减少集成复杂度60%：

| 原Agent | 合并后 | 理由 |
|---------|--------|------|
| Diagnostician + Assessment | **Evaluator** | 共享概念图谱和错误模式库，诊断和评估本质是对称操作 |
| Lecture Note + Reading | **Content Generator** | 都是长文本生成，prompt结构相同，资源类型用参数区分 |
| Exercise + Mind Map | **Exercise Generator** | 都是结构化输出+SymPy验证，输出格式不同但生成管线相同 |
| Orchestrator | **Orchestrator** (保留) | 意图路由 + Profile Builder逻辑合并到状态更新 |
| Socratic Coach | **Socratic Coach** (保留) | 核心差异化，保持独立 |

**对比**: 5Agent vs 9Agent — 集成边数从36条降到10条，每个Agent的调试时间翻倍但总数减半，总工期持平、风险更低。

## SSE Protocol

前后端通过SSE通信，以下事件类型覆盖全部交互场景：

```
event: message     — data: {"role": "system"|"coach"|"diagnostician"|"evaluator",
                            "content": "sin x ≈ x 的条件是...",
                            "agent": "socratic_coach"}
event: token       — data: {"content": "当", "agent": "content_generator"}
                     (流式逐token输出，前端累积后渲染)
event: resource    — data: {"type": "lecture_note"|"exercise"|"mind_map"|"reading",
                            "title": "泰勒展开讲义",
                            "content": "..."}
event: profile     — data: {"dimension": "knowledge",
                            "concept": "limit",
                            "score": 0.78,
                            "trend": "+0.12"}
event: status      — data: {"state": "diagnosing"|"coaching"|"generating"|"assessing",
                            "agents_completed": 2,
                            "agents_total": 4}
event: error       — data: {"code": "LLM_TIMEOUT"|"LLM_DOWN"|"MATH_ERROR"|"KB_ERROR",
                            "message": "正在切换备用模型，请稍候..."}
event: done        — data: {"session_id": "..."}
```

前端`StreamingMarkdown.tsx`根据`event`类型分发渲染：`token`追加到当前消息buffer、`message`创建新消息气泡、`resource`渲染资源卡片、`status`更新进度条、`error`展示snackbar提示。

## Architecture Overview

```
User → FastAPI/SSE → Orchestrator (LangGraph Supervisor)
                              ├── Profile Builder Agent
                              ├── Diagnostician Agent (盲区探测)
                              ├── Socratic Coach Agent (追问引擎)
                              ├── Resource Agents (Fan-out):
                              │   ├── Lecture Note Agent
                              │   ├── Exercise Agent
                              │   ├── Mind Map Agent
                              │   └── Reading Agent
                              ├── Assessment Agent
                              └── Quality Gate (管线过滤器: SymPy + 安全)
                                   ↓
                              Spark API / DeepSeek
                              ChromaDB (知识检索)
                              SymPy (数学验证)
                              SQLite (用户数据)
```

## Agent Roles

| Agent | 职责 | 模型选择 |
|-------|------|----------|
| Orchestrator | 意图路由、fan-out调度、结果聚合 | Spark Lite |
| Profile Builder | 学习画像构建与动态更新 | Spark Lite |
| Diagnostician | 对话诊断、概念图谱分析、盲区定位 | Spark Max |
| Socratic Coach | 多轮追问、难度自适应、心流调控 | Spark Max |
| Lecture Note Agent | 生成结构化教学讲义(LaTeX) | Spark Pro |
| Exercise Agent | 分层练习题+详细解答(SymPy验证) | Spark Pro |
| Mind Map Agent | Mermaid知识图谱生成 | Spark Pro |
| Reading Agent | 拓展阅读+实际应用连接 | Spark Pro |
| Assessment Agent | 作答评估、错误模式识别 | Spark Max |

> **Quality Gate** 不作为独立Agent，而是请求管线中的过滤器组件——所有LLM输出经SymPy数学验证+安全过滤后再返回用户。

## Agent I/O Contracts

每个Agent的输入输出由Pydantic model约束，LangGraph state update类型安全。

### Shared State Schema (LangGraph State)

```python
from typing import TypedDict, Literal, Annotated
from langgraph.graph.message import add_messages

class AgentState(TypedDict):
    messages: Annotated[list, add_messages]    # 对话历史，自动追加
    user_id: str
    profile: LearningProfile                    # 3维画像
    orchestrator_state: Literal["DIAGNOSE", "COACH", "GENERATE", "ASSESS", "DONE"]
    diagnosis: DiagnosisResult | None           # 最新诊断结果
    coach_round: int                            # 当前追问轮次
    coach_target_concept: str | None            # 追问聚焦的概念
    generated_resources: list[ResourceMeta]     # 已生成资源列表
    errors: list[str]                           # 错误日志
```

### Orchestrator

```
输入: AgentState (完整)
输出: {"orchestrator_state": "COACH" | "GENERATE" | ...}  (状态更新)
职责: 不直接调用LLM，仅做状态路由。根据diagnosis和coach_round决定下一步状态。
```

### Diagnostician

```python
class DiagnosticianInput(TypedDict):
    conversation_history: list[Message]          # 最近5轮对话
    profile: LearningProfile

class DiagnosticianOutput(TypedDict):
    blind_spots: list[BlindSpot]
    # BlindSpot = {concept: str, confidence: float, evidence: str,
    #              bloom_level: int, misconception_type: str}

class DiagnosticianResult(TypedDict):
    diagnosis: DiagnosticianOutput
    coach_target_concept: str                   # 最优先追问的概念
```

### Socratic Coach

```python
class SocraticCoachInput(TypedDict):
    target_concept: str                         # 当前追问概念
    blind_spot: BlindSpot                       # 诊断出的盲区
    coach_round: int                            # 当前轮次
    conversation_history: list[Message]
    kb_context: str                             # 从知识库检索的相关内容

class SocraticCoachOutput(TypedDict):
    question: str                               # 追问问题
    bloom_level: int                            # 当前追问层级(1-5)
    expected_concepts: list[str]                # 正确答案应涉及的概念
    rationale: str                              # 为什么问这个问题（调试用）
```

### Resource Agents (Lecture Note / Exercise / Mind Map / Reading)

```python
class ResourceAgentInput(TypedDict):
    concept: str                                # 资源聚焦的概念
    knowledge_nodes: list[KnowledgeNode]         # 从知识库检索的原始节点
    user_level: int                             # 学生当前Bloom层(1-5)
    resource_type: str                          # lecture_note | exercise | mind_map | reading

class ResourceAgentOutput(TypedDict):
    title: str
    content: str                                # Markdown/LaTeX/Mermaid 内容
    metadata: dict                              # {concept, type, difficulty, bloom_level}
```

### Assessment Agent

```python
class AssessmentInput(TypedDict):
    question: str                               # 原问题
    student_answer: str
    expected_concepts: list[str]                # Socratic Coach的expectation
    knowledge_nodes: list[KnowledgeNode]

class AssessmentOutput(TypedDict):
    is_correct: bool
    grasped_concepts: list[str]
    missed_concepts: list[str]
    error_pattern: str                          # 概念错误/计算错误/符号错误/逻辑错误
    bloom_level_score: int                      # 学生能回答对的最高Bloom层
```

### Profile Builder

```
不是独立LLM Agent，而是基于Diagnostician和Assessment产出的统计更新逻辑:
  - 知识掌握度 = 贝叶斯更新: P(mastery | new_evidence)
  - 易错点画像 = 错误频率累加
  - 学习历史 = 追加当前session记录
直接集成在 orchestrator.py 的 state update 中，无需API调用。
```



## Orchestrator: Intent Routing State Machine

Orchestrator是LangGraph Supervisor的入口节点，负责将用户输入路由到正确的Agent子图。状态机定义如下：

```
                    ┌─────────────┐
                    │   START     │
                    └─────┬───────┘
                          ↓
              ┌───────────────────────┐
              │   Intent Classifier   │
              │   (Spark Lite, ~50ms) │
              └───────┬───────────────┘
                      │
        ┌─────────────┼─────────────┐
        ↓             ↓             ↓
┌───────────┐  ┌───────────┐  ┌──────────────┐
│ DIAGNOSE  │  │  COACH    │  │   GENERATE   │
│  (诊断)   │  │  (追问)   │  │   (资源生成) │
└─────┬─────┘  └─────┬─────┘  └──────┬───────┘
      │              │              │
      ↓              ↓              ↓
┌───────────┐  ┌───────────┐  ┌──────────────┐
│Diagnostician│ │ Socratic  │  │ Fan-out to   │
│  Agent     │  │  Coach    │  │ 4 Resource   │
│            │  │  Agent    │  │   Agents     │
└─────┬─────┘  └─────┬─────┘  └──────┬───────┘
      │              │              │
      └──────────────┼──────────────┘
                     ↓
           ┌─────────────────┐
           │ ASSESS / PROFILE│
           │ (评估+画像更新) │
           └────────┬────────┘
                    ↓
           ┌─────────────────┐
           │   NEXT ACTION   │
           │ (Orchestrator   │
           │  决定下一步)    │
           └────────┬────────┘
                    │
        ┌───────────┼───────────┐
        ↓           ↓           ↓
   ┌────────┐ ┌────────┐ ┌────────┐
   │RETURN  │ │CONTINUE│ │ END    │
   │TO USER │ │COACH   │ │SESSION │
   └────────┘ └────────┘ └────────┘
```

**意图分类标签**:
| Intent | 触发条件 | 路由目标 |
|--------|---------|---------|
| `DIAGNOSE` | 首次会话、"开始诊断"、系统主动触发 | Diagnostician |
| `COACH` | 诊断完成后自动流转、用户回答问题后 | Socratic Coach |
| `GENERATE` | 追问发现根因后自动触发 | 4个Resource Agent fan-out |
| `ASSESS` | 用户提交答案、请求评估 | Assessment Agent |
| `CHAT` | 闲聊、疑问 | 直接LLM回复（不经过Agent） |
| `END` | 用户请求结束、超时 | Profile Builder更新画像后结束 |

**状态转换决策（量化门控）**:

| 转换 | 条件 | 门控逻辑 |
|------|------|---------|
| START → DIAGNOSE | 首次会话或`coach_round == 0` | 无条件 |
| DIAGNOSE → COACH | `len(blind_spots) >= 1` AND 诊断轮次 ≥ 3 | 最少3轮对话才判定"已诊断" |
| DIAGNOSE → DONE | 诊断轮次 ≥ 5 AND `len(blind_spots) == 0` | 5轮未发现盲区→放行 |
| COACH → GENERATE | 学生同一Bloom层卡住 ≥ 2次 AND `coach_round >= 4` | 2次同层卡壳→确认根因 |
| COACH → DONE | 学生L3+层连续正确 ≥ 2次 | 追问通过 |
| COACH → DIAGNOSE | `coach_round > 10` | 兜底：10轮追问无结论→重新诊断 |
| GENERATE → COACH | 所有Resource Agent完成 | 资源就绪→回到追问验证 |
| GENERATE → DONE | 资源生成完成 AND 学生请求结束 | 手动退出 |

**关键设计决策**: 
- 诊断→追问→生成→追问 形成自循环，直到COACH→DONE或超时退出
- Orchestrator本身不调用LLM，只做状态路由——避免小模型决策大流程的可靠性问题
- `coach_round`超限(>10)回到DIAGNOSE而非DONE——确保学生即使卡住也有路径继续

## Socratic Coach: Questioning Algorithm

Socratic Coach是系统的核心差异化引擎。采用**分层追问策略**，基于Bloom认知层次递进：

```
追问深度层级 (Bloom Taxonomy adapted for math):
┌────────────────────────────────────────────┐
│ L5 — 创造 (Create)                         │
│ "你能构造一个满足A但不满足B的反例吗？"     │
├────────────────────────────────────────────┤
│ L4 — 评价 (Evaluate)                       │
│ "为什么这个解法比那个更好？什么情况下失效？"│
├────────────────────────────────────────────┤
│ L3 — 分析 (Analyze)                        │
│ "证明sin x ~ x需要用到什么定理？条件是什么？"│
├────────────────────────────────────────────┤
│ L2 — 理解 (Understand)                     │
│ "用自己的话解释为什么lim sin x/x = 1成立"  │
├────────────────────────────────────────────┤
│ L1 — 记忆 (Remember)                       │
│ "等价无穷小替换的公式是什么？"             │
└────────────────────────────────────────────┘
```

**追问算法**:
1. **入口**: 从L2层开始提问（跳过L1纯记忆层，除非Diagnostician发现基础缺失）
2. **递进规则**: 学生连续2次正确回答 → 提升一层；学生卡住或错误 → 标记当前层为"边界层"
3. **根因定位**: 当学生在L2层卡住时，系统推断"概念理解有问题"；L3层卡住时推断"前置知识(泰勒展开)缺失"；L4层卡住时推断"应用迁移能力不足"
4. **话题生成策略**: 从知识库中检索"当前卡住概念"的所有前置依赖，按prerequisite链追溯到根因；同时检索该概念相关的"常见错误"节点，匹配学生的错误模式
5. **退出门槛**: 学生在L3层连续2次正确回答 → 追问通过；或追问轮次超过10轮 → 触发资源生成作为补救

**追问模板示例（极限/无穷小比较）**:
| 层次 | 示例问题 |
|------|---------|
| L2 | "sin x和x在x→0时谁趋近0更快？你能直观解释吗？" |
| L3 | "证明sin x ~ x需要洛必达法则还是泰勒展开？两种方法各有什么优缺点？" |
| L4 | "什么时候不能用等价无穷小替换？f(x)如果震荡很大还能替换吗？" |
| L5 | "构造一个函数，它在0附近和sin x行为类似但不满足等价无穷小条件" |



## Tech Stack

| Layer | Choice | Rationale |
|-------|--------|-----------|
| Multi-Agent | LangGraph (Supervisor) | 显式状态图、并行fan-out、checkpointing |
| Backend | Python 3.11+ / FastAPI | 异步原生、SSE流式、自动文档 |
| LLM Primary | 科大讯飞 Spark API (Lite/Pro/Max路由) | 比赛要求 |
| LLM Auxiliary | DeepSeek-v4 | 成本敏感场景补充 |
| Vector DB | ChromaDB | 零配置、嵌入式、规模足够 |
| Embedding | BGE-M3 (1024-dim) | 中英文混合数学文本最优 |
| Math Validation | SymPy | 符号计算验证所有生成公式和答案 |
| Frontend | Next.js 14 + shadcn/ui + KaTeX + Mermaid | 专业外观、数学渲染、流式展示 |
| Database | SQLite + SQLAlchemy | 零配置、开发友好 |
| Deployment | Docker Compose | 单命令启动，评委友好 |

## Project Structure

```
math-intelligent-tutor/
├── backend/
│   ├── app/
│   │   ├── main.py              # FastAPI entry + SSE streaming
│   │   ├── config.py            # Settings from .env
│   │   ├── api/                  # REST endpoints
│   │   │   ├── chat.py           # Chat + SSE streaming
│   │   │   ├── profile.py        # Learning profile CRUD
│   │   │   ├── generate.py       # Resource generation
│   │   │   ├── assess.py         # Assessment submission
│   │   │   └── path_planner.py   # Learning path
│   │   ├── agents/
│   │   │   ├── base.py           # Base agent (Spark binding)
│   │   │   ├── orchestrator.py   # LangGraph supervisor
│   │   │   ├── diagnostician.py  # Blind spot detection
│   │   │   ├── socratic_coach.py # Socratic questioning engine
│   │   │   ├── lecture_note.py   # Teaching doc generation
│   │   │   ├── exercise.py       # Exercise generation
│   │   │   ├── mind_map.py       # Knowledge graph generation
│   │   │   ├── reading.py        # Extended reading
│   │   │   ├── assessor.py       # Assessment + error analysis
│   │   │   ├── safety.py         # Quality gate
│   │   │   ├── graph.py          # LangGraph state graph
│   │   │   └── state.py          # Shared state schema
│   │   ├── knowledge/
│   │   │   ├── schema.py         # KnowledgeNode model
│   │   │   ├── loader.py         # YAML知识数据加载
│   │   │   ├── chunker.py        # LaTeX-aware chunking
│   │   │   ├── embedder.py       # BGE-M3 embedding
│   │   │   └── retriever.py      # 混合检索
│   │   ├── models/
│   │   │   ├── profile.py        # 3维学习画像Pydantic模型
│   │   │   └── db_models.py      # SQLAlchemy ORM
│   │   ├── services/
│   │   │   ├── math_service.py   # SymPy验证
│   │   │   └── profile_service.py # 画像更新逻辑
│   │   └── core/
│   │       ├── llm.py            # SparkLLM + ModelRouter
│   │       └── safety.py         # 内容安全管线
│   ├── data/
│   │   ├── seed/curriculum.yaml  # 极限与连续知识树
│   │   └── chromadb/             # 持久化向量库
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── src/app/
│   │   ├── page.tsx              # Landing
│   │   ├── chat/page.tsx         # 主对话界面
│   │   ├── profile/page.tsx      # 学习画像仪表盘
│   │   └── topics/page.tsx       # 知识树浏览
│   ├── src/components/
│   │   ├── chat/                 # ChatContainer, MessageBubble
│   │   │   └── StreamingMarkdown.tsx  # 流式KaTeX渲染
│   │   └── resources/            # LectureNoteCard, MindMapViewer等
│   ├── package.json
│   └── Dockerfile
├── docker-compose.yml
└── README.md
```

## Knowledge Base: 极限与连续

```
极限与连续/
├── 1.1-数列极限 (定义, ε-N语言, 收敛性判别, 四则运算, 夹逼准则, 单调有界)
├── 1.2-函数极限 (定义, ε-δ语言, 左右极限, 无穷小与无穷大比较, 极限运算法则)
├── 1.3-两个重要极限 (lim sinx/x=1, lim (1+1/x)^x=e, 等价无穷小替换)
├── 1.4-连续性 (连续定义, 间断点分类, 闭区间连续函数性质: 有界/最值/介值/零点)
└── 1.5-极限的应用 (渐近线, 函数作图, 连续函数的应用)
```

约15-20个知识节点（v1聚焦极限部分，连续性问题通过向量检索兜底），每个节点包含：定义(LaTeX)、定理、例题+详解、常见错误、前置依赖、关联概念。

## Learning Profile: 3 Dimensions (v1)

v1精简为3个核心维度，v2再扩展至7维：

1. **知识掌握度**：每个知识点的掌握分数(0-1)+置信度，贝叶斯更新
2. **易错点画像**：错误类型(概念/计算/符号/逻辑/前置缺失)、频率统计
3. **学习历史**：已完成主题、时间投入、资源使用记录

动态更新：显式(对话诊断) + 隐式(行为观察)，贝叶斯更新知识掌握分数。

> v2扩展维度：认知风格、学习目标、学习行为、数学基础。当前聚焦比赛demo可展示的最小闭环。

## Demo Script: 7-Minute Story Arc

**0:00-1:00** — 场景设定：一个学生在复习极限，系统通过简短对话建立画像
**1:00-3:00** — 诊断展开：Diagnostician发现学生在"等价无穷小替换"上只是记了公式
**3:00-5:00** — 苏格拉底追问：Socratic Coach追问"sin x ≈ x 什么时候成立？f(x)=0.1时还能替换吗？"，学生卡住，系统发现根因是"泰勒展开"概念缺失
**5:00-6:30** — 资源生成：多个Agent并行生成泰勒展开的讲义+思维导图+分层练习+动画脚本
**6:30-7:00** — 闭环展示：学生学完后再次追问通过，画像自动更新，学习路径调整

## Success Criteria

1. 完整诊断→追问→生成→评估闭环可用
2. 生成≥5种类型学习资源，每种有明确的教学目的
3. 极限知识库(≥15节点)，检索精度≥80%
4. 数学内容经SymPy验证无事实错误
5. 7分钟demo流畅展示完整story arc
6. 支持流式输出，无长时间白屏等待

## Error Handling & Degradation Strategy

| 故障场景 | 降级策略 | 用户感知 |
|---------|---------|---------|
| Spark API超时(>30s) | 自动重试1次，仍失败→切换DeepSeek | "正在切换备用模型，请稍候..." |
| Spark API全部不可用 | 降级至DeepSeek单模型模式 | 功能正常但追问质量可能略降 |
| 所有LLM不可用 | 返回预设错误提示+建议稍后重试 | "系统繁忙，请稍后再试" |
| ChromaDB检索失败 | 跳过RAG增强，仅用Agent内置知识 | 数学准确性可能下降，但对话不中断 |
| SymPy验证失败 | 标记"未验证"但不禁用资源生成 | 资源页显示"数学验证暂不可用"标签 |
| Fan-out部分Agent失败 | 已成功的Agent结果正常返回，失败的不展示 | 部分资源缺失，但不影响整体流程 |
| 单次对话超50轮 | 自动总结上下文，压缩历史 | 无感知 |
| 用户输入非数学内容 | 意图分类→CHAT，直接LLM回复 | 正常对话体验 |

**全局防护**:
- 所有Agent输出经过`Quality Gate`管线：数学一致性检查 + 内容安全过滤
- Spark API请求携带`temperature=0.3`（数学场景需低随机性），DeepSeek备用同样温控
- 知识库检索作为数学权威源，Agent生成内容与知识库冲突时以知识库为准

## Testing Strategy

### 分层测试金字塔

```
        ┌──────┐
        │ E2E  │  3个端到端场景
        ├──────┤
        │ Int. │  10+集成测试
        ├──────┤
        │ Unit │  30+单元测试
        └──────┘
```

### 单元测试
- **知识库**: chunker对LaTeX公式的切割精度（含极限/积分/求和等复杂公式）、embedder输出1024维验证、retriever混合检索top-k召回率
- **Agent**: 每个Agent的prompt模板渲染、输出schema校验（Pydantic验证）
- **数学验证**: SymPy验证器对等价无穷小、极限计算、导数计算的正确性
- **画像更新**: 贝叶斯更新公式数值验证

### 集成测试
- **诊断→追问→生成→评估闭环**: 给定预设学生答案序列，验证完整流程正确流转
- **Fan-out/Fan-in**: 4个Resource Agent并行生成后正确聚合
- **SSE流式**: 验证每个token正确传递、KaTeX公式不截断
- **知识库检索**: RAG增强后Agent回答引用正确知识节点
- **错误处理**: 模拟LLM超时、检索失败等异常场景的降级行为

### 端到端测试
1. **完整demo故事线**: 按7分钟demo脚本走一遍完整流程，验证产出
2. **边界case**: 学生连续10次错误回答、空输入、超长LaTeX公式输入
3. **多会话持久化**: 退出再进入，画像和对话历史正确恢复

## Distribution Plan

Docker Compose一键部署。GitHub仓库包含完整源码+种子数据+Dockerfile。README包含快速开始指南（3条命令启动）。演示视频独立提交。

CI/CD: GitHub Actions自动构建Docker镜像，推送至Docker Hub或GitHub Container Registry。

## Next Steps

1. **初始化项目结构**：FastAPI骨架 + Next.js骨架 + Docker Compose
2. **Spark API集成**：LLM Provider + ModelRouter + 流式输出
3. **知识库构建**：极限与连续YAML数据 + LaTeX-aware chunker + BGE-M3 embedding + ChromaDB
4. **核心Agent开发**：Diagnostician → Socratic Coach → Resource Agents
5. **LangGraph编排**：Supervisor图 + 状态管理 + fan-out/fan-in
6. **前端开发**：Chat UI + StreamingMarkdown + 资源展示组件 + 画像仪表盘
7. **集成测试+Demo录制**：端到端流程验证 + 7分钟演示视频

## What I noticed about how you think

- 你选择了"苏格拉底教练"而非"资源生成工具"——说明你要的不是checklist式的功能堆砌，而是一个有教学哲学的产品。评委能感受到这种不同
- "一章做透"的选择很有胆量——大多数人不敢做减法，担心功能不够多。但deep > broad在竞赛评审中往往是赢家策略，因为评委记住的是体验而非功能数量
- 你把手伸向"情绪感知+心流"这个10x版本，但不执着于现在实现——知道北极星在哪，也知道第一步迈多远，这是工程判断力
- 当我说"vs ChatGPT/数学老师"时你选了vs数学老师——说明你对标的是人类教学体验，而不是另一个AI工具，这让产品定位天然高于竞品
